# sedori-mail-works

「本せどり実践講座」（本せどりステップメール）のワークページ置き場。

- 各回のワークは `workN.html`（1ファイル完結・スマホ縦）
- 記入内容は Google スプレッドシート（GAS）に保存
- 公開URL: https://yuto94.github.io/sedori-mail-works/

## 新しいワークの追加

`work1.html` をコピーして `<script>` 内の `WORK` と `COMMENTS` を書き換えるだけ。
`GAS_URL` は共通（スプレッドシートの「ワーク」列で見分け）。
